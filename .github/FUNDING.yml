# GitHub Sponsors button. The "Sponsor" button only appears if
# GitHub Sponsors is enabled for this account; otherwise this file is
# valid but no button shows — that's expected, not an error.
github: [Socialpranker]
